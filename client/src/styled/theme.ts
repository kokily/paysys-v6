import type { CustomTheme } from '../types/styled-components';

const theme: CustomTheme = {
  colors: {
    member: '#1098ad',
    associate: '#68a614',
    general: '#e47112',
  },
};

export default theme;
